[project]
name = "speech-verifier-backend"
version = "0.1.0"
description = "Backend API for the Hearvana speech verifier."
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

